
以下是我后端的我后端接口返回了一个`ArrayList<Map<String, String>>`的数据，但前端代码似乎与它不兼容，请修改前端：
```
[
    {
        "drugBName": "Ribavirin",
        "drugAName": "Abacavir",
        "ddiDescription": "Combining drugAName with drugBName may lead to a greater inhibition of platelet function, thus amplifying the anticoagulant activities of drugBName and reducing the risk of thrombosis."
    },
    {
        "drugBName": "Aprepitant",
        "drugAName": "Abemaciclib",
        "ddiDescription": "Patients on a regimen including both drugAName and drugBName are at a heightened risk for bleeding, potentially because these drugs collectively diminish the efficacy of the clotting cascade."
    }
]
```
```typescript
<ul>
    {batchSearchResult.items.map((item, idx) => (
    <li key={idx}>
    <strong>Drug A:</strong> {item.drugAName}, <strong>Drug B:</strong> {item.drugBName}, <strong>DDI:</strong> {item.ddiDescription}
    </li>
    ))}
</ul>
```
```typescript
export interface BatchSearchResult {
    items: Array<{
        drugAName: string,
        drugBName: string,
        ddiDescription: string
    }>; // 当前页的数据项
}
```