

在DrugMapper.xml中，你给的代码是
```xml
<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.DrugMapper">

    <insert id="insert" parameterType="com.example.demo.entity.Drug" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO drugs (drugbank_id, name, category, chemical_formula, description)
        VALUES (#{drugbankId}, #{name}, #{category}, #{chemicalFormula}, #{description})
    </insert>

    <!-- 如果需要，可以添加其他 SQL 映射 -->
    <!-- 例如，如果你有 find 方法 -->
    <select id="findByDrugbankId" parameterType="string" resultType="com.example.demo.entity.Drug">
        SELECT * FROM drugs WHERE drugbank_id = #{drugbankId}
    </select>

</mapper>
```
但我在mapper新建xml文件，显示的是
```xml
<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd">
</beans>
```