<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ddisearch.mapper.DrugInfoMapper">

    <insert id="batchInsertDrugInfo" parameterType="java.util.List">
        INSERT INTO cjk.drug_info (order_id, drugbank_id, name, category, chemical_formula, smiles, description, related_drugs, pharmacodynamics, action_mechanism, protein_binding, metabolism)
        VALUES
        <foreach collection="list" item="drug" separator=",">
            (#{drug.orderId}, #{drug.drugbankId}, #{drug.name}, #{drug.category}, #{drug.chemicalFormula}, #{drug.smiles}, #{drug.description}, #{drug.relatedDrugs}, #{drug.pharmacodynamics}, #{drug.actionMechanism}, #{drug.proteinBinding}, #{drug.metabolism})
        </foreach>
    </insert>

    <select id="selectDrugInfoByName" parameterType="String" resultType="com.ddisearch.entity.Drug">
        SELECT order_id, drugbank_id, name, category, chemical_formula, smiles, description, related_drugs, pharmacodynamics, action_mechanism,protein_binding,metabolism FROM cjk.drug_info WHERE name = #{name}
    </select>

</mapper>
